#include <iostream>
#include <string>

using namespace std;

int main()
{
	// iostream speed up
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	// variable declaration
	string input;
	int count{};

	// input
	cin >> input;

	// checking 크로아티아
	for (int i = 0; i < input.length(); i++)
	{
		input.replace();
	}

	// counting

	// output

}
